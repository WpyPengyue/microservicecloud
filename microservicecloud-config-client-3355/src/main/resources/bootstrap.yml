spring:
  cloud:
    config:
      name: microservicecloud-config-client #需要从github读取的资源名称，注意没有yml后缀名
      profile: test
      label: master
      uri: http://config-3344.com:3344 #本服务启动后去找3344号服务，通过SpringCloudConfig获取github服务地址